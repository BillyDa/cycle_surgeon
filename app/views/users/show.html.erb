<h1>Welcome!</h1>

<% if @user.surgeon %>
 <%= link_to 'Welcome Surgeon!', user_url(@user.id) %><br />
 <%= link_to 'See all current tickets', tickets_url%>
<% else %>
 <%= link_to 'Welcome Cyclist!', user_url(@user.id) %><br />
 <%= link_to 'Submit a ticket', new_ticket_url %><br />
 <% end %>

<%= link_to 'Logout', login_url, method: 'delete' %>
